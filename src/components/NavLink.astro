---
import type { HTMLAttributes } from 'astro/types';

interface Props extends HTMLAttributes<'a'> {}

const { ...props } = Astro.props;
---

<a {...props} class="decoration-violet-600 decoration-2 hover:underline">
  <slot /></a
>
