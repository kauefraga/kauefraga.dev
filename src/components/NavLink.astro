---
import type { HTMLAttributes } from 'astro/types';

interface Props extends HTMLAttributes<'a'> {}

const { ...props } = Astro.props;
---

<a
  {...props}
  class="rounded border-2 border-transparent transition-all hover:border-violet-800 md:px-4 md:py-2"
  ><slot /></a
>
