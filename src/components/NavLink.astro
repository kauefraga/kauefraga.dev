---
import type { HTMLAttributes } from 'astro/types';

interface Props extends HTMLAttributes<'a'> {}

const { ...props } = Astro.props;
---

<a
  {...props}
  class="underline decoration-violet-600 underline-offset-2 transition-all hover:underline-offset-[6px] md:px-4 md:py-2"
  ><slot /></a
>
