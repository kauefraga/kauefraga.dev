---
import type { HTMLAttributes } from 'astro/types';

interface Props extends HTMLAttributes<'a'> {}

const { ...props } = Astro.props;
---

<a
  {...props}
  class="rounded-sm px-4 py-2 transition-colors hover:bg-violet-800 hover:text-white"
  ><slot /></a
>
