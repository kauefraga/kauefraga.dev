---
import type { HTMLAttributes } from 'astro/types';

interface Props extends HTMLAttributes<'a'> {}

const { ...props } = Astro.props;
---

<a
  {...props}
  class="rounded border-2 border-transparent px-4 py-2 transition-all hover:border-violet-800"
  ><slot /></a
>
